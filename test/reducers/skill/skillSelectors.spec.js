import { map } from 'lodash';

import skillSelectors from '../../../app/reducers/skill/skillSelectors';

describe('skillSelectors', () => {
  describe('this is a dummy test that should be removed', () => {
    it('shouldnt be here, and yet it is', () => {
      expect('this').toEqual('this');
    });
  });
});
